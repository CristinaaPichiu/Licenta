spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jwt_security_ok
    username: postgres
    password: al5gtuv*
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
